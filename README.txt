The Z-Ray AWS plugin provides information on the Amazon instance Zend Server is running on, including the instance ID, the host name, public key, and more.

This plugin only functions properly in an Amazon environment.